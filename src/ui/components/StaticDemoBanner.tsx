export function StaticDemoBanner() {
  return (
    <div className="static-demo-banner">
      STATIC PREVIEW — this export has no backend. Search, Execute, and the live log are disabled.
      Clone the repo and run <code>npm run dev</code> for the real terminal.
    </div>
  );
}
